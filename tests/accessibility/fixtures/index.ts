//Import all fixtures here for better clarity, eliminating the need to individually export every file. Instead, simply export the index file.
export{expect, test} from "../fixtures/homePage"
// export{ expect, test } from "../fixtures/fixtureFile1"
// export { expect, test } from "../fixtures/fixtureFile2"